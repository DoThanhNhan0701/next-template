import CustomersPage from "@/components/pages/admin/customers";

export const metadata = {
  title: "Customers | Admin",
  description: "Manage customers in the system",
};

export default function Page() {
  return <CustomersPage />;
}
